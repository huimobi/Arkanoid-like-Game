package project.com.Viewer.MainMenu;

import com.googlecode.lanterna.TextColor;
import project.com.Viewer.WriteText;
import project.com.gui.GUI;

/*public class OptionsViewer {
    private final WriteText text;
    public OptionsViewer(WriteText text) {
        this.text= text;
    }

    public void draw(Option model, GUI gui) { //need a option model class
        String entryText = switch (model.getType()) {
            case START_GAME -> "Start";
            case SETTINGS -> "Settings";
            case EXIT -> "Exit";
        };
        WriteText.draw(entryText, (int) model.getPosition().x(), (int) model.getPosition().y(), color, gui);
    }
}*/
